'use strict';


function math(a) {
  return (a) ;
}
const Price = (20);
const Iva = (20 * (21 / 100) );
const priceTotal = Price + Iva;
console.log(`El precio sin IVA ${Price} , el IVA (21%) ${Iva} y el total ${priceTotal}`);