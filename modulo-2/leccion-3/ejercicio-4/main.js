'use strict';

 const ageDog = 4;
 const ageHuman = 15;
 const ageDog2 = 9;
 const ageDog3 = ageHuman +  ageDog2;
 const ageDog4 = ageDog - 2;

 if (ageDog === 1 ) {
 console.log (`Tu perro tiene ${ageHuman} años`)
 }
else if (ageDog  === 2) { 
 console.log (`Tu perro tiene ${ageHuman + ageDog2} `)
    }
else if (ageDog  >= 3) {
    console.log (`Tu perro tiene ${ageDog3 + (5 * ageDog4)} `)
    }