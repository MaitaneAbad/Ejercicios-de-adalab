'use strict';

const input = document.querySelector('.input');


function handlerEventListener(event){
    event.currentTarget.value
    console.dir(handlerEventListener)
}

input.addEventListener('change', handlerEventListener)